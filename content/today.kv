<TodayBar>
    size_hint: 1, 0.1
    orientation: "vertical"
    MDToolbar:
        title: "Today"
        left_action_items: [["menu", lambda x: root.menu_click(app)]]
        right_action_items: [["card-search", lambda x: root.other()], ["dots-vertical", lambda x: root.other()]]

<TodayBox>
    do_scroll_x: False
    do_scroll_y: True

    MDLabel:
        halign: "center"
        size_hint_y: None
        height: self.texture_size[1]
        padding: 10, 10
        text:
            'cool entry right here\n' * 100

<Today>
    name: "Today"
    MDBoxLayout:
        orientation: "vertical"
        TodayBar:
        TodayBox: